1 小程序内置了很多对象，属性，方法，接口
2 内置了每个对象都有很多属性
3 设置属性有些是必须的
4 使用组件时，组件上的属性有些是必填的，这个不是必须使用这个属性的意思，而是如果你使用了这个属性，就必须给值
5 前端开发三步走 ：1 选择组件 2 修改组件样式 3 使用组件事件
6 数据绑定
	1 文件直接写入
	2 内存和服务器：通过HTTP请求，调用服务器的接口，传输或者获取数据
	3 内存到页面： 页面获取内存中的数据是通过 {{}} 语法获取值
	4 页面到内存：页面是通过标签DOM节点的方式，挂载数据，根据dom节点获取数据：比如input标签的name属性绑定数据到DOM节点上
	5 内存到JS文件：通过挂载的dom对象节点，或者其对象，从它的对象里获取数据，然后js中可以对数据进行操作，重点在于对象和对象结构
7 data
	1 赋值：this.setData({meg:'赋值'})
	2 取值：this.data.msg，
	注意：this.msg 是undefined，this是页面对象，data是页面里的一个对象，msg是data里的一个属性，
	我以为这样可以取值，但是这样是不可以的，你觉得对象结构是这样的，其实不是，
	你又是如何觉得page对象的结构应该是这样呢
8 页面加载：
	如何在页面加载内调用方法：也就是如何让页面加载的时候，方法已经被加载完成了，这可能吗，可以实现吗
9 success 回调函数里不能使用this
10 onload 函数内 如果想要调用自定义的函数，需要适应this.方法名，如果没有this指向就是没有定义
11 如何从小程序request请求返回数据中获取一个数组，如何操作返回响应的这个对象
	1 如何返回的数据格式，js没有将数据封装到一个属性上是无法根据属性获取的
	2 通过反序列化，将字符串发序列化成对象，然后操作对象 var obj = JSON.parse("json字符串")
12 如何在列表中数据的按钮上绑定当前列表的主键ID
	1 按钮本身没有提供一个属性用户绑定当前列表的主键ID
13 业务绑定事件的时候是否可以传入一个变量值
	有一个内置的对象dataset ,任何标签都可以绑定这个属性来传递参数
	虽然button标签上没有这个属性，但是这个属性是小程序的所有的标签也都可以使用
14 es6 拼接字符串的方法：拼接变量
	1 ''两个单引号：'字符串 ${变量名}'：错误·· ，是两个反引号，单引号是错的
15 功能
 1 最最最基本的增删改查
 2 分页查询
 
16 下拉刷新
	1 遇到问题，把上拉加载当成下拉刷新，怎么上拉都没有用，折腾了很长时间，最后发现上拉刷新一直是有用的，但是我以为是下拉加载
17 下拉加载
	1 下拉加载了新的数组到原先的数组中，这些数据就会立刻展示在页面上吗？小程序会监听data数据的变化？
	2 加载到data中的数据会自动展示到页面上
	3 上拉加载的数值50代表什么意思
18 页面跳转：路由
	1 分几种页面：1 tab 页面 2 小程序中普通页面 3 其它小程序的页面
	2 navigateTo,redirectTo 只能打开非Tab页面
	3 switchTab只能打开tab页面
	4 
19 交互方式
	1 应用和服务端的数据交互
	2 应用内页面之间的数据交互
	3 应用内组件和方法的数据交互
20 字符串拼接变量
	"字符串{{变量}}"，在小程序中变量可以通过双花括号的方式直接在双引号内引用
21 设置全局变量
	1 globData 全局变量
	2 缓存
	3 定义一个类，每个类引用它
		导出对象 export {对象名称}    引入对象  import {对象名称} from "文件相对路径",
22 npm 安装lin-ui
	1 生成package：cd 到小程序项目根目录下，在这里生成包依赖配置
	1 npm init:生成package.json 文件，这个文件内配置项目的信息，依赖的名称，版本等
	3 安装依赖：npm install ~ (npm i)简写
	2 构建依赖：小程序有一个build npm 菜单，点一下，就会构建项目
23 slot 插槽
	1 通过这个插槽，页面引用这个组件的时候，可以在这个插槽，加入自己的内容
	2 插槽相当于游戏机的插槽，想要玩什么游戏，买个卡，插入插槽
	3 模板就相当于游戏机，用来展示画面用的
24 自定义组件
	1 目的：为了复用，代码好维护
	2 js:
		a Component({}) ：创建一个component对象
		b properties : 设置component对象上的属性，这里的属性值设置不能没有定义，不然报错，定义的语法和data还有点不一样
			1 变量：绑定数据 pro-a，pro-b,用户调用处，动态绑定数据
		c data{}:私有数据，用户渲染模板，和调用处的data数据域，互相独立
		d methods{}: 组件自己业务逻辑
		e options{}:组件的配置，组件自身有的一些属性，可以通过在这里配置起作用：
			1 multipleSlots: true // 在组件定义时的选项中启用多slot支持
			2 styleIsolation: 'isolated' //表示启用样式隔离
		f externalClasses: ['class'], // 可以将 class 设为 externalClasses,引用外部样式
	3 wxml:<view><slot></slot></view>
	4 引用：wxml:<component-tag-name></component-tag-name>,
			json:
				{
					"usingComponents": 
					{
						"my-component": "/components/component-tag-name"
						//组件引用时起的别名：组件文件夹地址，是文件夹还是指向文件夹内？指向文件夹内的具体文件
					}
				}
		注：微信小程序开发文档，找了半天没有找到关于页面如何引用组件的内容
			创建自定义组件，有关于这个，但是组件模块，并没有创建自定义组件的入口
			入口是在页面配置，usingComponents 属性配置，点进去才是创建自定义组件
			这样是不是很坑？创建自定义组件不放在，自定义组件模块，第一个入口？
			每个大模块点进去，页面配置的内容，还是不一样的，真尼玛烦，什么破文档，乱七八糟的
		全局引用：很多地方用到同一个组件，在app.json里引用，方式和index.json页面里引用一样属性名，格式
	5 组件data:data 数据如何获取？1 框架自动给，根据name自动映射匹配？ 2 手动赋值？
	6 问题：
		1 报错信息：报错看全了，因为主要问题，引发的次要问题，没看到主要报错，盯着次要报错，始终无法理解为什么会这样
			其实因为内部错误，外面所有地方都挂了，但外面其实代码编写和语法都没有问题，只是因为逻辑上内不错了，逻辑上
			外部也错了，次要报错，都是一条逻辑上的逻辑不支持错误，因为根本错误引发出来的，盯着逻辑很无法理解
		2 如果组件定义了properties:pro-a，不知道如何使用，文档也查不到，没有定义
		3 文档虽然有基础知识，但是没有流程逻辑解释啊，就像组件data数据的获取，我从哪里查找，怎么知道
		4 文档说明了A是什么怎么用，B是什么，怎么用，但是没有A-B,A如何使用B啊，里面很多细节，都不讲，靠猜吗？
25 数组 :这种语法格式，文档没有说明，我是从哪里知道这些的呢，除了视频，还能从哪里学习它
properties{
	gril:Array //没有初始化属性值
	grilV:{
		type:Array,
		value:[1,2,3]//这里空就没有初始值，但这里可以进行初始值
	}
}
data{
	gril:[]
}
26 类 对象：导入，导出
	1 用来做全局对象复用